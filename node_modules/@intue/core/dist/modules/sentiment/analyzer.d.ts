import { SentimentProvider } from '../../providers/types';
export interface SentimentAnalysisConfig {
    providers?: SentimentProvider[];
    thresholds?: {
        positive: number;
        negative: number;
    };
}
/**
 * Sentiment analysis module for crypto assets
 */
export declare class SentimentAnalyzer {
    private providers;
    private thresholds;
    /**
     * Create a new sentiment analyzer
     * @param config - Configuration for the analyzer
     */
    constructor(config?: SentimentAnalysisConfig);
    /**
     * Add a sentiment provider
     * @param provider - The provider to add
     */
    addProvider(provider: SentimentProvider): void;
    /**
     * Analyze social sentiment for a crypto asset
     * @param symbol - Asset symbol (e.g., 'BTC')
     * @returns Sentiment analysis result
     */
    analyzeSocialSentiment(symbol: string): Promise<any>;
    /**
     * Analyze market sentiment based on price action
     * @param data - Market data
     * @returns Market sentiment analysis
     */
    analyzeMarketSentiment(data: any): any;
}
