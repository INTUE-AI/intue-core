import { Agent } from './agent';
/**
 * Configuration for the INTUE runtime
 */
export interface RuntimeConfig {
    agentName?: string;
    options?: {
        logger?: any;
        [key: string]: any;
    };
}
/**
 * Runtime environment for INTUE agents
 */
export declare class Runtime {
    private agent;
    /**
     * Create a new runtime instance
     * @param agent - The agent to run
     */
    constructor(agent: Agent);
    /**
     * Initialize a new INTUE runtime
     * @param config - Runtime configuration
     * @returns Initialized runtime
     */
    static init(config: RuntimeConfig): Promise<Runtime>;
    /**
     * Start the runtime
     */
    start(): Promise<void>;
    /**
     * Stop the runtime
     */
    stop(): Promise<void>;
    /**
     * Get the agent instance
     */
    getAgent(): Agent;
}
