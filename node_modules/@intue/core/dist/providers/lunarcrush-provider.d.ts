import { DataProvider } from './types';
export interface LunarCrushConfig {
    apiKey: string;
    baseUrl?: string;
}
/**
 * Provider for LunarCrush API data
 */
export declare class LunarCrushProvider implements DataProvider {
    name: string;
    private apiKey;
    private baseUrl;
    private initialized;
    /**
     * Create a new LunarCrush provider
     * @param config - Configuration for the provider
     */
    constructor(config: LunarCrushConfig);
    /**
     * Initialize the provider
     */
    initialize(): Promise<void>;
    /**
     * Fetch data from LunarCrush
     * @param query - Query parameters
     * @returns The fetched data
     */
    fetchData(query: any): Promise<any>;
    /**
     * Get social sentiment for a specific asset
     * @param symbol - Asset symbol (e.g., 'BTC')
     * @returns Social sentiment data
     */
    getSocialSentiment(symbol: string): Promise<any>;
    /**
     * Analyze sentiment for the given query
     * This method is required by the SentimentProvider interface
     */
    analyzeSentiment(data: any): Promise<any>;
}
